import React from "react"
import { Stack, Button } from "@mui/material"

interface ButtonStackProps {
  //define property types
}

const ButtonStack = (): React.ReactNode => {
  return <Stack direction="row"></Stack>
}

export default ButtonStack
